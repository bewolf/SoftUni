function fitnessRates(dayOfWeek, service, time) {
    let price = 0;
    let hour = time.split(".");

    switch (dayOfWeek) {
        case 'Monday':
            switch (service) {
                case 'Fitness':
                case 'Sauna':
                case 'Instructor':
            }
        case 'Tuesday':
            switch (service) {
                case 'Fitness':
                case 'Sauna':
                case 'Instructor':
            }
        case 'Wednesday':
            switch (service) {
                case 'Fitness':
                case 'Sauna':
                case 'Instructor':
            }
        case 'Thursday':
            switch (service) {
                case 'Fitness':
                case 'Sauna':
                case 'Instructor':
            }
        case 'Friday':
            switch (service) {
                case 'Fitness':
                case 'Sauna':
                case 'Instructor':
            }
        case 'Saturday':
            switch (service) {
                case 'Fitness':
                case 'Sauna':
                case 'Instructor':
            }
        case 'Sunday':
            switch (service) {
                case 'Fitness':
                case 'Sauna':
                case 'Instructor':
            }
    }
    console.log(hour)
}
fitnessRates('Monday', 'Saunda', 9.30)
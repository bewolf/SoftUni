function solve(input) {
    let flightType = input[0];
    let townName = input[1];
    let time = input[2];
    let flightNum = input[3];
    let gateNum = input[4];
    console.log(`${flightType}: Destination - ${townName}, Flight - ${flightNum}, Time - ${time}, Gate - ${gateNum}`)
}

solve()